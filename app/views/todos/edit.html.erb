<h1>Edit Todo</h1>
<%= form_with model: @todo do |f| %>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="actions">
    <%= f.submit "Update Todo" %>
  </div>
<% end %>